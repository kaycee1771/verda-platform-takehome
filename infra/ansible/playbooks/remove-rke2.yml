---
- name: Phase 14 gate for RKE2 teardown
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop until teardown is explicitly authorized
      ansible.builtin.fail:
        msg: Phase 14 RKE2 teardown is not implemented.

# Network Policy Test Framework

The Phase 1 framework defines ownership and expected verdict vocabulary without claiming a cluster
test exists. Phase 3 proves only the pre-Kubernetes host WireGuard, MTU, firewall, and reboot
boundary. Executable Cilium and Kubernetes NetworkPolicy cases begin only after their owning cluster
and policy phases install those components.

---
- name: Phase 4 gate for RKE2 installation
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop until RKE2 installation is authorized
      ansible.builtin.fail:
        msg: Phase 4 RKE2 installation is not authorized or implemented.

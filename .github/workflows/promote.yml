---
name: Promote - Phase 10 gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  phase-gate:
    runs-on: ubuntu-24.04
    steps:
      - name: Stop before unimplemented promotion phase
        shell: bash
        run: |
          echo 'Digest-based promotion requires Phase 10.' >&2
          exit 64

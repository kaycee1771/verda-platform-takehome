---
- name: Phase 3 gate for host preparation
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop until host configuration is authorized
      ansible.builtin.fail:
        msg: Phase 3 host preparation is not implemented.

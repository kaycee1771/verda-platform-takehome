# Known Limitations

The decisive current limitations are the shared management/workload failure domain, no managed load balancer/DNS, dense rollout capacity, bundled Harbor database, protected rather than public Grafana access, namespace rather than cluster-level environment separation, and no regional DR test.

See [docs/known-limitations.md](../../docs/known-limitations.md) for consequences and production improvements.

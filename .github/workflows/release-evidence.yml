---
name: Release evidence - Phase 9 gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  phase-gate:
    runs-on: ubuntu-24.04
    steps:
      - name: Stop before unimplemented release phase
        shell: bash
        run: |
          echo 'Release evidence publication requires Phase 9.' >&2
          exit 64

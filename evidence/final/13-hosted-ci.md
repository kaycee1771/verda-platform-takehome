# Hosted CI

Protected GitHub validation was green for runtime PRs #29 through #34. The final submission PR must also pass the required validation workflow before merge; the final run link is recorded in the release notes.

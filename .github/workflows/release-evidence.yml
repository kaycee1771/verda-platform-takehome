---
name: Release evidence - Phase 17 gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  phase-gate:
    runs-on: ubuntu-24.04
    steps:
      - name: Stop before unimplemented release phase
        shell: bash
        run: |
          echo 'Release evidence publication requires Phase 17.' >&2
          exit 64

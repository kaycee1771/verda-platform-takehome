# GitOps Boundary

This tree reserves the Phase 4 Argo CD root, AppProject, ApplicationSet, cluster, and repository
ownership boundaries. Placeholder YAML documents contain no Kubernetes object and therefore cannot
be applied accidentally.

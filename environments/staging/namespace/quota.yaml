---
apiVersion: v1
kind: ResourceQuota
metadata: {name: platform-budget, namespace: demo-staging}
spec:
  hard:
    requests.cpu: 500m
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "10"
    persistentvolumeclaims: "2"

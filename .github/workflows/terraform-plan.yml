---
name: Terraform plan - Phase 2 gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  phase-gate:
    runs-on: ubuntu-24.04
    steps:
      - name: Stop before unimplemented infrastructure phase
        shell: bash
        run: |
          echo 'Terraform planning requires Phase 2 and an approved credential boundary.' >&2
          exit 64

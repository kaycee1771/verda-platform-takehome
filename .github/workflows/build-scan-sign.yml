---
name: Build scan sign - Phase 6 gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  phase-gate:
    runs-on: ubuntu-24.04
    steps:
      - name: Stop before unimplemented supply-chain phase
        shell: bash
        run: |
          echo 'Image build, scan, and signing require Phase 6.' >&2
          exit 64

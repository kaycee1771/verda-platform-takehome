---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: platform-demo-registry
  namespace: demo-dev
  annotations:
    sealedsecrets.bitnami.com/strict: "true"
spec:
  encryptedData:
    .dockerconfigjson: REQUIRED_SEALED_CIPHERTEXT_DEMO_DEV_REGISTRY
  template:
    metadata: {name: platform-demo-registry, namespace: demo-dev}
    type: kubernetes.io/dockerconfigjson
